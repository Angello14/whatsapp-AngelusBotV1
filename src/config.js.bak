const path = require('path')

const PREFIX = '/'
const BOT_EMOJI = '👺️'
const TEMP_FOLDER = path.resolve(__dirname, '..', 'assets', 'temp')

module.exports = {
  BOT_EMOJI,
  PREFIX,
  TEMP_FOLDER
}
